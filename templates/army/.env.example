# Clawup Secrets — copy to .env and fill in values
# See clawup.yaml 'secrets' section for which keys map where

# ── Required ─────────────────────────────────
TAILSCALE_AUTH_KEY=
TAILNET_DNS_NAME=
TAILSCALE_API_KEY=
HCLOUD_TOKEN=
BRAVE_API_KEY=
ANTHROPIC_API_KEY=

# ── Agent: Juno (pm) ──────────────────────
PM_LINEAR_API_KEY=
PM_LINEAR_WEBHOOK_SECRET=
# PM_LINEAR_USER_UUID=  # auto-resolved by `clawup deploy`
PM_SLACK_BOT_TOKEN=
PM_SLACK_APP_TOKEN=
PM_GITHUB_TOKEN=

# ── Agent: Titus (eng) ──────────────────────
ENG_LINEAR_API_KEY=
ENG_LINEAR_WEBHOOK_SECRET=
# ENG_LINEAR_USER_UUID=  # auto-resolved by `clawup deploy`
ENG_SLACK_BOT_TOKEN=
ENG_SLACK_APP_TOKEN=
ENG_GITHUB_TOKEN=

# ── Agent: Scout (tester) ──────────────────────
TESTER_LINEAR_API_KEY=
TESTER_LINEAR_WEBHOOK_SECRET=
# TESTER_LINEAR_USER_UUID=  # auto-resolved by `clawup deploy`
TESTER_SLACK_BOT_TOKEN=
TESTER_SLACK_APP_TOKEN=
TESTER_GITHUB_TOKEN=
